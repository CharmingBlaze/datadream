package sdk

// RaylibVersion is the bundled raylib release (latest stable).
const RaylibVersion = "6.0"

// RaylibReleaseURL is the GitHub release tag base for downloads.
const RaylibReleaseURL = "https://github.com/raysan5/raylib/releases/download/6.0"

// LLVMMingwVersion is the pinned llvm-mingw release (Windows native toolchain).
const LLVMMingwVersion = "20251118"

// LLVMOrgVersion is the pinned LLVM release for Linux/macOS native toolchains.
const LLVMOrgVersion = "19.1.7"
